pipeline {
    agent any
    stages {
        stage("Terraform-instance") {
            steps {
               git branch: 'main', url: 'https://github.com/ajeetrai266/Terraform-instance.git'
            }
        }
    }
}
